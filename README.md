# To-Do App

A simple to-do list app. Add tasks, mark them done, filter by status, and clear completed items. Tasks are saved in your browser's local storage, so nothing leaves your machine.

## Run it

Open `index.html` in a browser. No build step, no server, no dependencies.
